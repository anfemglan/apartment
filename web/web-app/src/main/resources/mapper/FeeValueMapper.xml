<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atafl.lease.web.app.mapper.FeeValueMapper">

    <select id="selectListById" resultType="com.atafl.lease.web.app.vo.fee.FeeValueVo">
        select fv.id,
               fv.name,
               fv.unit,
               fv.fee_key_id,
               fk.name fee_key_name
        from fee_value fv
                 left join fee_key fk on fk.id = fv.fee_key_id and fk.is_deleted = 0
        where fv.is_deleted = 0
          and fv.id in (select fee_key_id
                        from apartment_fee_value
                        where fv.id = fee_value_id)
    </select>
</mapper>
